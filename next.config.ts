import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'randomuser.me', 'avatars.githubusercontent.com']
  },
};

export default nextConfig;
